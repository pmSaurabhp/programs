
#include<conio.h>
#include<stdio.h>

struct employee
{ int empid;
char name[10];
float sal;
};
void main()
{clrscr();
struct employee b={101,"sks",1000};  //compile time
struct employee a;       // value from user
scanf("%d %s %f",&a.empid,&a.name,&a.sal);
 printf("%d %s %f ",b.empid,b.name,b.sal);
 printf("%d %s %f",a.empid,a.name,a.sal);

getch();
}