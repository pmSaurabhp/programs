#include<stdio.h>
#include<conio.h>
#include<stdlib.h>
struct node
{int data;
 struct node *next;
};
void main()
{ int i,p;
 struct node *n,*start,*temp,*newnode,*first,*second,*third;
  clrscr();

  printf("Enter no. in first,second,third");
  scanf("%d %d %d",&first->data,&second->data,&third->data);
  start=first;
  first->next=&second->data;           // second=&second->data
  second->next=third;
  third->next=NULL;
  n=start;
  while(n!=NULL)
  {  printf("%d",n->data);
    n=n->next;
  }
   printf("%u %u %u %u %u %u %u %u %u %u %u",&first,first,&first->data,&first->next,first->next,second,&second->data,&second->next,second->next,third,third->next);

    getch();
  }
