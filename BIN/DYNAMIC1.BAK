#include<conio.h>
#include<stdio.h>
#include<stdlib.h>
void main()
{
int i,*ptr,n;
clrscr();
 printf("no. of elements");
 scanf("%d",&n);
  ptr=(int*)malloc(n*sizeof(int));
  for(i=0;i<n;i++)
  { ptr[i]=i+1;
  }
  for(i=0;i<n;i++)
 { printf(" %d",ptr[i]);
 }
 getch();
}